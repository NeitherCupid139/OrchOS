/**
 * Stub for `cloudflare:workers` module when running without @cloudflare/vite-plugin.
 * In the fast dev mode, D1 bindings won't be available.
 */
export const env = {} as Record<string, unknown>;
